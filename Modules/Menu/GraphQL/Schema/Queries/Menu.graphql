extend type Query {
    menuListing(input: MenuListingInput): [Menu]
    @field(resolver: "Modules\\Menu\\GraphQL\\Queries\\MenuQuery@menuListing")
    menuDetails(menu_id: ID!): Menu
    @field(resolver: "Modules\\Menu\\GraphQL\\Queries\\MenuQuery@menuDetails")
}

input MenuListingInput {
    keyword: String
}